"""
PEGA predictor modules.

Each module in this sub-package defines one or more concrete subclasses
of :class:`~pega.base.BasePredictor`.  The registry discovers them
automatically — no manual registration is required.
"""
