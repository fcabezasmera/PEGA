# Core — always required
numpy>=1.24
pandas>=2.0
biopython>=1.81
tqdm>=4.65
requests>=2.31

# Deep learning predictors (AMPnet, AMP_CG)
# pip install "pega-amp[deep]"
tensorflow>=2.13
torch>=2.0
transformers>=4.35

# modlAMP predictors (RF, SVM)
# pip install "pega-amp[modlamp]"
scikit-learn>=1.3
joblib>=1.3
modlamp>=4.3

# External tools — not pip-installable
# ampir  (predictors 3 & 4) : install.packages("ampir")  in R
# macrel (predictor 6)       : conda install -c bioconda macrel
# AMPlify (predictors 7 & 8) : conda install -c bioconda amplify
# amPEPpy (predictor 2)      : pip install ampep
